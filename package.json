{
  "name": "lightning-reporting-mcp",
  "version": "0.2.0",
  "private": true,
  "description": "Read-only Lightning Transportation reporting MCP server for ChatGPT",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "compile": "npm run build --prefix mcp-server",
    "dev": "npm run dev --prefix mcp-server",
    "start": "npm start --prefix mcp-server",
    "lint": "npm run lint --prefix mcp-server",
    "test": "npm test --prefix mcp-server"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "@types/node": "^24.5.2",
    "jose": "^6.2.12",
    "typescript": "^7.0.2",
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "tsx": "^4.23.13",
    "vitest": "^5.0.0"
  }
}
